# Graphics and Diagrams Manifest: Applied Network Programmability (NET-226)

**Author:** Dr. Frazier A. Smith  
**Institution:** Central Piedmont Community College (CPCC)  
**Textbook:** *Applied Network Programmability: Automating Cisco Infrastructure with Python, APIs, and NetDevOps*  
**Format:** PNG and SVG high-resolution diagrams for Antora AsciiDoc OER documentation site (`graphics/chXX/gX-X-...`)  

---

## 1. Master Graphics Inventory Summary

| Chapter | Active Figure Slots | Primary Diagram Categories |
|:---:|:---:|:---|
| **Ch. 1: Foundations** | 6 (G1.1 – G1.6) | Paradigm shift, workstation architecture, Linux FHS/permissions, pipes/streams, Python control flow, OOP class model |
| **Ch. 2: DevNet Ecosystem** | 6 (G2.1 – G2.6) | DevNet portal pillars, sandbox taxonomy (Always-On vs Reservable), VPN topology, resource evaluation flowchart, Campus-A topology, agile backlog |
| **Ch. 3: Software Practices** | 7 (G3.1 – G3.7) | MVC architecture, Git mechanics/staging, feature branching/PRs, merge conflict anatomy, data serialization comparison (JSON/YAML/XML), TDD loop, test pyramid |
| **Ch. 4: REST APIs** | 6 (G4.1 – G4.6) | API architecture comparison (REST/RPC/Webhooks), HTTP transaction anatomy, status code decision tree, auth spectrum (Basic/API Key/Bearer/OAuth), Postman architecture, Python `requests` lifecycle |
| **Ch. 5: Network Diagnostics** | 8 (G5.1 – G5.8) | End-to-end packet trace, L2 forwarding/MAC table, L3 routing/longest prefix, L4 TCP 3-way handshake, IP services (DNS/DHCP/NTP), edge devices (FW/NAT/LB), 3-plane separation, network constraints matrix |
| **Ch. 6: Containers & CI/CD** | 6 (G6.1 – G6.6) | Deployment spectrum (Metal/VM/Containers), Docker architecture & layers, container networking/ports, CI pipeline architecture, secrets pipeline, OWASP threat topography |
| **Ch. 7: IaC with Ansible** | 7 (G7.1 – G7.7) | IaC lifecycle & idempotency, config management comparison, Ansible control node/inventory topology, playbook anatomy, Jinja2 templating flow, pyATS state verification, change management/rollback |
| **Ch. 8: Model-Driven & Capstone** | 7 (G8.1 – G8.7) | Model-driven stack, YANG tree schema hierarchy, NETCONF protocol layers & RPCs, RESTCONF URI/HTTP mapping, Catalyst Center architecture, ChatOps Webex bot flow, Capstone solution blueprint |
| **Total Figures** | **51 Figures** | **High-Fidelity Engineering Visuals across 8 Chapters** |

---

## 2. Chapter-by-Chapter Graphics Specifications

### Chapter 1: Foundations: Development Environment, Linux Systems, and Python

| ID | Ch. & Sec. | Type | File Path | Detailed Visual Specification & Generation Prompt |
|:---:|:---:|:---:|:---|:---|
| **G1.1** | Ch 1 §1.2 | Conceptual Comparison | `graphics/ch01/g1-1-cli-vs-programmability.png` | **Prompt:** "Create a clean split-panel architectural diagram comparing traditional network management with software-driven network programmability. Left panel: 'Traditional Imperative CLI' showing a tired network engineer manually typing commands into five separate black terminal windows connected via serial console and SSH to individual routers; text callout: 'Box-by-box, error-prone, zero version history, slow rollbacks'. Right panel: 'Modern Network Programmability' showing a developer workstation running Python, Git, and Ansible, pushing a single declarative configuration data model through an automated testing pipeline to manage an entire fleet of routers simultaneously; text callout: 'Automated, version-controlled, validated, reproducible'. Flat modern technical style, light background, muted Cisco blue and slate gray palette." |
| **G1.2** | Ch 1 §1.3 | Architecture Diagram | `graphics/ch01/g1-2-workstation-architecture.png` | **Prompt:** "Create a technical architecture diagram of a modern network developer workstation based on VS Code. Outer box represents the Developer Workstation (NETLAB+ DEVASC VM). Inside, show Visual Studio Code as the primary IDE container with key extension blocks: 'Python & Pylance', 'GitLens', 'YAML', and 'Remote-SSH'. Show the integrated terminal running a Python 3.10 virtual environment (`.venv`) isolated from system Python. At the bottom, show the integrated interactive debugger with Call Stack, Variables watch window, and Breakpoints on a Python script. Flat vector diagram, crisp lines, light theme, accent colors in soft blue and green." |
| **G1.3** | Ch 1 §1.4 | Technical Infographic | `graphics/ch01/g1-3-linux-fhs-permissions.png` | **Prompt:** "Create an infographic illustrating Linux filesystem navigation and POSIX permissions for network engineers. Top half: Linux Filesystem Hierarchy Standard (FHS) tree showing root `/` branching into `/etc` (network configs), `/var/log` (syslog), `/home/developer` (workspace), and `/usr/bin` (executables). Bottom half: An annotated breakdown of a Linux permission string `-rwxr-xr-- 1 developer netops 4096 interface_audit.py`. Break into four colored callouts: File Type (`-`), User (`rwx` = 7), Group (`r-x` = 5), Others (`r--` = 4). Show binary and octal conversion tables clearly. Clean technical typography, high contrast, light background." |
| **G1.4** | Ch 1 §1.4 | Process Flowchart | `graphics/ch01/g1-4-linux-streams-pipes.png` | **Prompt:** "Create a technical process diagram illustrating standard Linux I/O streams, pipes, and redirection. Show a running process box in the center with three streams: 'Standard Input (stdin / fd 0)' entering from keyboard, 'Standard Output (stdout / fd 1)' flowing to terminal, and 'Standard Error (stderr / fd 2)' flowing to error log. Show a pipe operator `\|` taking stdout into a secondary filter process `grep 'GigabitEthernet'` and then redirecting `>` to `active_ports.txt`. Clean vector style, clear directional arrows, light background." |
| **G1.5** | Ch 1 §1.5 | Logic Flowchart | `graphics/ch01/g1-5-python-control-flow-collections.png` | **Prompt:** "Create a programming logic diagram illustrating Python control flow and data collection handling for network inventories. Show a list of router dictionaries entering a `for device in inventory:` iteration loop. Inside the loop, show an `if device['status'] == 'active':` conditional branch. True branch enters a `try...except (ConnectionError, TimeoutError):` block executing `connect_to_device()`. On success, append state to a summary dictionary; on exception, log error to an exceptions list. Clean flowchart symbols, pastel decision diamonds, dark gray text on light background." |
| **G1.6** | Ch 1 §1.5 | Class Diagram | `graphics/ch01/g1-6-oop-network-model.png` | **Prompt:** "Create a UML class diagram modeling network infrastructure components in Python. Base class `NetworkDevice` with attributes `hostname: str`, `ip_address: IPv4Address`, `platform: str`, `is_reachable: bool` and methods `check_connectivity() -> bool`, `get_facts() -> dict`. Two derived classes: `CiscoIOSDevice` adding `enable_secret: str`, `run_command(cmd: str)` and `NexusDevice` adding `vdc_id: int`, `execute_nxapi()`. Standard UML formatting with visibility markers, clean border boxes, muted slate blue header accents." |

---

### Chapter 2: The DevNet Ecosystem: Developer Resources, Sandboxes, and Project Scoping

| ID | Ch. & Sec. | Type | File Path | Detailed Visual Specification & Generation Prompt |
|:---:|:---:|:---:|:---|:---|
| **G2.1** | Ch 2 §2.2 | Ecosystem Map | `graphics/ch02/g2-1-devnet-four-pillars.png` | **Prompt:** "Create an architectural ecosystem diagram showing the four core pillars of the Cisco DevNet developer portal. Central hub: 'Cisco DevNet Developer Portal (developer.cisco.com)'. Surrounding quadrants: (1) 'DevNet Learning Labs' (guided hands-on tutorials and micro-learning), (2) 'DevNet Sandboxes' (free cloud-hosted hardware and software labs), (3) 'DevNet Code Exchange' (curated open-source automation repositories and sample code), (4) 'DevNet Community & Support' (developer forums, Webex spaces, API ticket support). Crisp iconography for each pillar, professional corporate layout, Cisco blue branding on light background." |
| **G2.2** | Ch 2 §2.3 | Comparison Matrix | `graphics/ch02/g2-2-sandbox-taxonomy.png` | **Prompt:** "Create a side-by-side comparison diagram illustrating DevNet Sandbox types. Left panel: 'Always-On Sandboxes' showing public cloud availability, instant 24/7 access over HTTPS, shared multi-tenant environment, read-only/simulated write permissions, ideal for REST API GET requests and quick script validation. Right panel: 'Reservable Sandboxes' showing scheduled reservation calendar, private isolated pod access via AnyConnect VPN, dedicated virtual router topologies (CSR1000v, Nexus 9k), full root/privilege-15 access, supporting persistent configuration changes and deep testing. Vector graphics, clear checkmarks and constraint badges." |
| **G2.3** | Ch 2 §2.3 | Network Topology | `graphics/ch02/g2-3-reservable-vpn-topology.png` | **Prompt:** "Create a network topology diagram showing remote developer access to a Cisco DevNet Reservable Sandbox. On the left: 'Developer Workstation' running AnyConnect VPN client. In the center: 'Public Internet' with an encrypted SSL/IPsec VPN tunnel crossing to 'DevNet Cloud Gateway'. On the right: 'Isolated Private Sandbox Subnet (10.10.20.0/24)' containing a Cisco CSR1000v router (management IP 10.10.20.48), a Linux jump host, and a virtual switch. Annotate transport ports: Port 443 (HTTPS RESTCONF), Port 830 (NETCONF over SSH), Port 22 (SSH CLI). Technical network styling, standard Cisco network icons." |
| **G2.4** | Ch 2 §2.4 | Decision Flowchart | `graphics/ch02/g2-4-resource-evaluation-flowchart.png` | **Prompt:** "Create a decision flowchart for evaluating third-party network automation code from GitHub or blogs. Start: 'Discovered Automation Script'. Decision 1: 'Is source authoritative?' (Official Cisco repo vs unverified user). Decision 2: 'Is API version current?' (Check for deprecation notices, v1 vs v2). Decision 3: 'Are credentials exposed?' (Hardcoded API keys vs environment variables). Decision 4: 'Is license compatible?' (MIT/Apache 2.0 vs GPL/All Rights Reserved). Final terminal outcomes: 'Approved for Enterprise Pipeline' (green) or 'Reject & Refactor' (red). Clean top-to-bottom flowchart, soft color coding." |
| **G2.5** | Ch 2 §2.5 | Project Architecture | `graphics/ch02/g2-5-campus-a-project-topology.png` | **Prompt:** "Create a logical topology diagram for the 'Campus-A' Network Change Automation project. Top tier: 'Developer Workstation' executing the Network Change Automation Service. Middle tier: 'Campus-A Distribution Layer' with two redundant Cisco Catalyst/CSR routers connected via 802.1Q trunk links. Bottom tier: 'Campus Access Layer' with three department VLANs: VLAN 10 (Faculty - 10.1.10.0/24), VLAN 20 (Student Labs - 10.1.20.0/24), and VLAN 99 (Management - 10.1.99.0/24). Right sidebar shows external integrations: 'Local Mock API Simulator' and 'Operations Alert Channel'. Clean enterprise network diagram." |
| **G2.6** | Ch 2 §2.5 | Roadmap Diagram | `graphics/ch02/g2-6-project-milestone-lifecycle.png` | **Prompt:** "Create a horizontal project milestone roadmap showing the 8-week progression of the Network Change Automation project. Five milestone cards on a timeline: Week 2 'Milestone 1: Project Charter & Backlog', Week 3 'Milestone 2: Git Repo & Tested Parser Module', Week 4 'Milestone 3: REST API Client & Interface Contract', Week 6 'Milestone 4: Docker Container & CI Pipeline Run', Week 7 'Milestone 5: Idempotent Ansible Playbook & Device State Verification', and Week 8 'Capstone Demonstration Packet'. Sleek modern project roadmap, numbered milestones, blue and teal accents on white." |

---

### Chapter 3: Modern Software Practices: Design Patterns, Version Control, Data Formats, and TDD

| ID | Ch. & Sec. | Type | File Path | Detailed Visual Specification & Generation Prompt |
|:---:|:---:|:---:|:---|:---|
| **G3.1** | Ch 3 §3.2 | Architectural Diagram | `graphics/ch03/g3-1-mvc-network-automation.png` | **Prompt:** "Create an architectural diagram illustrating the Model-View-Controller (MVC) design pattern applied to network automation. 'Controller' block receives user CLI arguments or webhook triggers, queries the 'Model' block, and instructs the 'View' block. 'Model' block manages raw network device state, inventory objects, and configuration schemas. 'View' block renders output into multiple formats: terminal ASCII tables, HTML audit dashboards, or Webex Teams alert cards. Show bidirectional arrows with annotations: 'Fetches device state', 'Formats presentation', 'Updates model'. Clean flat design, light gray container boxes, blue accents." |
| **G3.2** | Ch 3 §3.3 | Technical Flowchart | `graphics/ch03/g3-2-git-three-trees.png` | **Prompt:** "Create a technical diagram showing Git's three-tier local architecture. Left to right: (1) 'Working Directory' (untracked and modified files on disk), arrow labeled `git add` to (2) 'Staging Area / Index' (snapshots prepared for commit), arrow labeled `git commit` to (3) 'Local Repository / HEAD' (immutable commit DAG history), arrow labeled `git push` to (4) 'Remote Repository (GitHub)'. Below, show reverse arrows labeled `git checkout / git restore`, `git reset`, and `git pull`. High-clarity vector diagram, clear typography, light background." |
| **G3.3** | Ch 3 §3.3 | Branching Diagram | `graphics/ch03/g3-3-git-feature-branch-workflow.png` | **Prompt:** "Create a Git feature branching lifecycle diagram. Top line: `main` branch with stable commit nodes (v1.0, v1.1). Branch point splits into `feature/vlan-automation` showing three successive developer commits. Arrow shows a 'Pull Request (PR)' submitted for peer review. Two review checklist icons: 'Automated Tests Passed' and 'Peer Approved'. Branch merges cleanly back into `main` via a merge commit. Bottom hotfix branch demonstrates rapid patching. Standard Git graph visualization, colored nodes, crisp lines on white background." |
| **G3.4** | Ch 3 §3.4 | Technical Illustration | `graphics/ch03/g3-4-merge-conflict-anatomy.png` | **Prompt:** "Create an annotated visual breakdown of a Git merge conflict inside a Python network script. Show a code editor window with conflict markers: `<<<<<<< HEAD` highlighting local branch code (`timeout = 10`), separated by `=======` divider line, followed by remote branch code (`timeout = 30`) ending at `>>>>>>> feature/timeout-patch`. Color-code local changes in blue, incoming changes in green. Below, show the resolved file with the combined, chosen code cleanly formatted. Annotated callouts explaining each marker line." |
| **G3.5** | Ch 3 §3.5 | Rosetta Stone Chart | `graphics/ch03/g3-5-data-formats-comparison.png` | **Prompt:** "Create a three-panel comparative 'Rosetta Stone' diagram showing the exact same network interface configuration represented in three data formats. Left: JSON (`{\"interface\": {\"name\": \"GigabitEthernet1\", \"ip\": \"10.1.1.1\", \"enabled\": true}}`). Center: YAML (`interface:\n  name: GigabitEthernet1\n  ip: 10.1.1.1\n  enabled: true`). Right: XML (`<interface><name>GigabitEthernet1</name><ip>10.1.1.1</ip><enabled>true</enabled></interface>`). Highlight syntax elements: JSON brackets and commas, YAML whitespace indentation, XML opening/closing tags and namespaces. Clean syntax-highlighted editor panels side by side." |
| **G3.6** | Ch 3 §3.7 | Process Loop | `graphics/ch03/g3-6-tdd-cycle.png` | **Prompt:** "Create a circular process diagram illustrating the Test-Driven Development (TDD) cycle for network programming. Three circular stages connected by clockwise arrows: (1) 'RED: Write a failing unit test' (red badge), (2) 'GREEN: Write minimum Python code to pass test' (green badge), (3) 'REFACTOR: Clean up code, remove duplication, preserve test passes' (blue badge). In the center: 'Repeat for boundary and error conditions'. Modern circular workflow, crisp typography, clean vector iconography." |
| **G3.7** | Ch 3 §3.7 | Pyramid Diagram | `graphics/ch03/g3-7-test-pyramid-networking.png` | **Prompt:** "Create a three-tiered testing pyramid tailored to network automation. Bottom tier (broadest): 'Unit Tests' (testing isolated Python functions, JSON parsing, regex matching; runs in milliseconds, zero hardware dependency). Middle tier: 'Integration Tests' (testing API client calls against mock servers or sandboxes; validates HTTP status codes and payloads). Top tier (narrowest): 'End-to-End System Tests' (executing changes against real hardware or virtual topologies; validates physical device state). Annotate speed and cost gradients along the sides. Clean isometric pyramid, soft shading." |

---

### Chapter 4: REST APIs and Secure Network Integration

| ID | Ch. & Sec. | Type | File Path | Detailed Visual Specification & Generation Prompt |
|:---:|:---:|:---:|:---|:---|
| **G4.1** | Ch 4 §4.2 | Architectural Matrix | `graphics/ch04/g4-1-api-architecture-comparison.png` | **Prompt:** "Create a comparative architectural diagram contrasting four network API styles: (1) 'REST' (stateless, resource-oriented, standard HTTP verbs over URIs), (2) 'RPC / gRPC' (action-oriented, function calls like `reboot()`, Protocol Buffers over HTTP/2), (3) 'SOAP' (XML-envelope wrapped messages, strict WSDL contracts, heavy legacy protocol), (4) 'Webhooks' (inverted event-driven architecture, push notification via HTTP POST on event trigger). Use four distinct illustrated cards with pros, cons, and primary network use cases. Professional technical infographic layout." |
| **G4.2** | Ch 4 §4.3 | Annotated Diagram | `graphics/ch04/g4-2-http-transaction-anatomy.png` | **Prompt:** "Create an annotated technical diagram illustrating the complete anatomy of an HTTP request and response transaction. Top box: 'HTTP Request' dissecting Method (`GET`, `POST`), URI (`https://api.cisco.com/v1/devices`), Request Headers (`Authorization: Bearer <token>`, `Accept: application/json`), and Request Body (JSON payload). Bottom box: 'HTTP Response' dissecting Status Line (`HTTP/1.1 200 OK`), Response Headers (`Content-Type: application/json`, `Retry-After: 60`), and Response Body (structured JSON array of device objects). Connecting arrows show client-server interaction over TLS. High-precision vector diagram." |
| **G4.3** | Ch 4 §4.3 | Decision Tree | `graphics/ch04/g4-3-http-status-codes-tree.png` | **Prompt:** "Create a decision tree flowchart for diagnosing network API errors based on HTTP status codes. Top: 'Received API Response'. Branch 2xx: 'Success (200 OK, 201 Created, 204 No Content)' -> Parse JSON body. Branch 4xx: 'Client Error' -> Sub-branches: 400 (Malformed JSON/syntax), 401 (Invalid/expired token), 403 (Insufficient RBAC permissions), 404 (Resource URI does not exist), 429 (Rate limited -> inspect `Retry-After` header). Branch 5xx: 'Server Error' -> 500/502/503 -> Check controller health / retry with backoff. Clear flowchart symbols, light background." |
| **G4.4** | Ch 4 §4.4 | Security Diagram | `graphics/ch04/g4-4-authentication-mechanisms.png` | **Prompt:** "Create an infographic illustrating the spectrum of API authentication mechanisms from basic to enterprise-grade. (1) 'HTTP Basic Auth' (base64 encoded username/password in header, insecure without TLS), (2) 'API Keys' (long-lived static secret in custom header, easy but high leakage risk), (3) 'Bearer Tokens' (short-lived access tokens obtained from login exchange), (4) 'OAuth 2.0 Authorization Flow' (Client, Authorization Server, Resource Server; token exchange without sharing user credentials). Annotate security level and rotation complexity for each. Sleek corporate security visual." |
| **G4.5** | Ch 4 §4.5 | UI Architecture | `graphics/ch04/g4-5-postman-environment-architecture.png` | **Prompt:** "Create an architectural diagram of a Postman testing workspace. Show a hierarchical Postman Collection on the left containing requests (`Login`, `List Devices`, `Configure VLAN`). In the center, show 'Variable Scopes' stacking from Global to Environment (`{{base_url}}`, `{{auth_token}}`) to Collection to Local. On the right, show the execution engine: 'Pre-request Script' generating timestamps, sending HTTP request, and 'Tests Tab' running automated assertions (`pm.response.to.have.status(200)`). Modern developer tool styling on light background." |
| **G4.6** | Ch 4 §4.6 | Logic Flowchart | `graphics/ch04/g4-6-python-requests-lifecycle.png` | **Prompt:** "Create a software flowchart illustrating a production-grade Python `requests` client lifecycle. Process starts by loading API credentials from environment variables (`os.environ`). Builds request headers with Bearer token. Submits request in a `try...except` block with explicit `timeout=10`. Evaluates status code: If 200, parses JSON and returns data. If 429 (Rate Limit), reads `Retry-After` header, executes exponential backoff `time.sleep()`, and retries. If connection error, retries up to 3 times before raising a custom application exception. Clean vector flowchart." |

---

### Chapter 5: Network Fundamentals and Application Flow Troubleshooting

| ID | Ch. & Sec. | Type | File Path | Detailed Visual Specification & Generation Prompt |
|:---:|:---:|:---:|:---|:---|
| **G5.1** | Ch 5 §5.1 | Packet Flow Diagram | `graphics/ch05/g5-1-end-to-end-packet-trace.png` | **Prompt:** "Create an end-to-end network packet flow diagram tracing an API call from a developer laptop to a cloud controller. Sequence steps: (1) Workstation sends DNS query (UDP 53) to local DNS server to resolve `api.meraki.com`. (2) Workstation initiates TCP 3-way handshake (TCP 443) via local switch and default gateway router. (3) Edge router performs NAT (PAT) translating private IP to public IP. (4) Packet traverses enterprise firewall which inspects state. (5) Cloud load balancer terminates TLS and forwards HTTP request to internal API microservice. Show both the OSI layer and protocol header encapsulations along the path. Comprehensive technical diagram." |
| **G5.2** | Ch 5 §5.2 | Logic Flowchart | `graphics/ch05/g5-2-switch-mac-learning-forwarding.png` | **Prompt:** "Create a decision flowchart showing how an Ethernet switch processes an incoming frame. Step 1: Ingress frame arrives on Port 1. Step 2: Switch inspects Source MAC -> updates MAC Address Table with Port 1 mapping. Step 3: Switch inspects Destination MAC in table. Decision: 'Is destination MAC known?' If YES: Unicast forward directly to matching egress port. If NO: Flood frame out all ports in the same VLAN except Port 1 (Unknown Unicast Flooding). Include visual representation of the MAC Address Table columns (VLAN, MAC Address, Type, Port). Clean technical layout." |
| **G5.3** | Ch 5 §5.3 | Decision Flowchart | `graphics/ch05/g5-3-router-forwarding-longest-prefix.png` | **Prompt:** "Create a decision flowchart illustrating a router's Layer 3 packet forwarding logic. Step 1: Packet arrives; verify Layer 2 FCS checksum and Layer 3 IP header checksum; decrement TTL by 1 (drop if TTL=0). Step 2: Inspect Destination IP. Step 3: Query IP Routing Table using 'Longest-Prefix Match' algorithm. Show three competing routes: `10.0.0.0/8`, `10.1.0.0/16`, and `10.1.20.0/24` -> highlight `/24` as winning match. Step 4: Resolve Next-Hop IP via ARP cache. Step 5: Re-encapsulate into new Layer 2 Ethernet frame with destination MAC of next-hop and transmit out egress interface. Technical network styling." |
| **G5.4** | Ch 5 §5.4 | Sequence Diagram | `graphics/ch05/g5-4-tcp-three-way-handshake.png` | **Prompt:** "Create a technical sequence diagram illustrating the TCP Three-Way Handshake and port multiplexing. Left timeline: 'Python Automation Client (Source Port 52140)'. Right timeline: 'Network Device API Server (Dest Port 443)'. Message 1: Client sends `SYN` (Seq=0). Message 2: Server replies `SYN-ACK` (Seq=0, Ack=1). Message 3: Client sends `ACK` (Seq=1, Ack=1) establishing `ESTABLISHED` state. Show subsequent encrypted TLS Client Hello and Application Data transfer. Below, show connection teardown via `FIN` and `ACK` exchanges. Professional network protocol sequence chart." |
| **G5.5** | Ch 5 §5.5 | Service Architecture | `graphics/ch05/g5-5-core-ip-services.png` | **Prompt:** "Create a multi-panel diagram showing the core IP services supporting network automation. Panel A: 'DNS Resolution' showing hostname to IP mapping and caching. Panel B: 'DHCP DORA Process' showing Discover, Offer, Request, and Acknowledgment exchange assigning IP, mask, gateway, and DHCP Option 43 (controller IP). Panel C: 'NTP Time Synchronization' showing hierarchical stratum clock sources ensuring synchronized timestamps across router logs, syslog servers, and TLS certificate validation. Clean technical infographic." |
| **G5.6** | Ch 5 §5.5 | Edge Components | `graphics/ch05/g5-6-firewall-nat-loadbalancer.png` | **Prompt:** "Create a technical diagram illustrating the operational functions of network edge devices on automation traffic. (1) 'Stateful Firewall' showing connection state table allowing return traffic while blocking unauthorized ports. (2) 'Network Address Translation (PAT)' showing translation table mapping internal private socket `192.168.1.50:45123` to public socket `203.0.113.5:60001`. (3) 'Reverse Proxy / Load Balancer' terminating public TLS connections and distributing requests across three backend API server nodes using round-robin. High-clarity network diagram." |
| **G5.7** | Ch 5 §5.6 | Architectural Diagram | `graphics/ch05/g5-7-network-planes-separation.png` | **Prompt:** "Create a conceptual architectural diagram illustrating the separation of network planes. Top tier: 'Management Plane' (SSH, Telnet, SNMP, RESTCONF, NETCONF) where humans and scripts interact with device configurations. Middle tier: 'Control Plane' (OSPF, BGP, STP, ARP) calculating forwarding tables and route paths via software CPU. Bottom tier: 'Data Plane' (ASICs, TCAM, packet switching) forwarding customer packets at line-rate in hardware. Contrast with 'SDN Architecture' where the Control Plane is decoupled and centralized into a controller. Clean block diagram." |
| **G5.8** | Ch 5 §5.7 | Matrix Chart | `graphics/ch05/g5-8-network-constraints-matrix.png` | **Prompt:** "Create a comprehensive reference matrix illustrating how physical network constraints degrade automation scripts. Four rows: 'Bandwidth Saturation', 'High Latency (RTT > 200ms)', 'Jitter (Latency Variance)', 'Packet Loss (> 2%)'. Three columns: 'Physical/Protocol Cause', 'Symptom in Python Automation Script' (timeouts, slow data transfers, socket reset errors), and 'Engineering Mitigation Strategy' (chunking payloads, tuning socket timeouts, implementing backoff/retries, using local caches). Alternating row shading, crisp typography." |

---

### Chapter 6: Application Deployment, Continuous Integration, and Security

| ID | Ch. & Sec. | Type | File Path | Detailed Visual Specification & Generation Prompt |
|:---:|:---:|:---:|:---|:---|
| **G6.1** | Ch 6 §6.2 | Architectural Comparison | `graphics/ch06/g6-1-deployment-models-spectrum.png` | **Prompt:** "Create a four-tier visual comparison of application deployment models: (1) 'Bare Metal' (App -> OS -> Physical Hardware), (2) 'Virtual Machines' (App -> Guest OS -> Hypervisor -> Host OS/Hardware), (3) 'Docker Containers' (App -> User Libs -> Container Engine -> Shared Host OS Kernel), (4) 'Serverless / Edge' (Function Code -> Managed Cloud Execution Environment). Annotate boot time (minutes vs seconds vs milliseconds), resource footprint, and portability for each model. Modern technical illustration, light background." |
| **G6.2** | Ch 6 §6.3 | Architecture Diagram | `graphics/ch06/g6-2-docker-architecture-layers.png` | **Prompt:** "Create a technical diagram of Docker container architecture and image layering. Bottom to top: 'Base OS Layer (python:3.11-slim)', 'Working Directory Layer (`WORKDIR /app`)', 'Dependencies Layer (`RUN pip install -r requirements.txt`)', 'Application Source Layer (`COPY src/ .`)', and top 'Thin Read/Write Container Layer'. Show how image layers are read-only and cached, enabling sub-second builds. On the right, show Docker Engine interacting with local image cache and Docker Hub registry. Clean vector graphics." |
| **G6.3** | Ch 6 §6.3 | Network & Storage | `graphics/ch06/g6-3-container-networking-ports.png` | **Prompt:** "Create a technical diagram showing Docker container networking and port redirection. Show a 'Host Linux System' with physical network interface `eth0 (192.168.1.100)`. Inside, show a 'Docker Bridge (`docker0: 172.17.0.1`)' providing private network namespace. Show a container running a Python REST service bound to private IP `172.17.0.2:5000`. Illustrate port forwarding rule `-p 8080:5000` mapping external requests on host port 8080 through NAT to container port 5000. Also show a persistent volume mount `-v /var/log/audit:/app/logs`. High precision diagram." |
| **G6.4** | Ch 6 §6.4 | Pipeline Diagram | `graphics/ch06/g6-4-ci-pipeline-architecture.png` | **Prompt:** "Create a Continuous Integration (CI) pipeline diagram for network automation using GitHub Actions. Left: Developer pushes commit to Git repository. Triggers 'CI Runner Environment'. Stage 1: 'Checkout Code & Setup Python 3.11'. Stage 2: 'Linting & Syntax Validation (flake8, black)'. Stage 3: 'Execute Unit Tests (`unittest`)'. Stage 4: 'Security Scan (detect-secrets, bandit)'. Stage 5: 'Build Container Image (`docker build`)'. Stage 6: 'Publish Container Artifact'. Show green checkmark badges on passing steps and a red alert branching on failed unit tests. Modern DevOps pipeline graphic." |
| **G6.5** | Ch 6 §6.5 | Security Architecture | `graphics/ch06/g6-5-secrets-management-pipeline.png` | **Prompt:** "Create a security architecture diagram showing safe secrets management across the development lifecycle. Left: 'Developer Workstation' using `.env` file containing local API keys, explicitly blocked from Git via `.gitignore`. Center: 'Remote Repository' holding code only, zero credentials. Top: 'Encrypted Secrets Vault (GitHub Secrets / HashiCorp Vault)' securely storing production tokens. Right: 'CI/CD Pipeline Runner' securely injecting secrets as environment variables into memory at runtime. Red warning banner: 'Never commit secrets to source control'. High-contrast security visual." |
| **G6.6** | Ch 6 §6.6 | Threat Topography | `graphics/ch06/g6-6-owasp-top-10-network-threats.png` | **Prompt:** "Create a technical infographic illustrating the OWASP Top 10 vulnerabilities most relevant to network automation services. Five highlighted threat vectors: (1) 'Command Injection' (passing unvalidated user input directly to router CLI execution strings), (2) 'Broken Authentication' (weak static tokens or lack of session expiration), (3) 'Security Misconfiguration' (default passwords, unencrypted HTTP endpoints), (4) 'Vulnerable Dependencies' (outdated third-party Python packages with known CVEs), (5) 'Insufficient Logging & Monitoring' (silent failures during configuration pushes). Include mitigation badges for each." |

---

### Chapter 7: Infrastructure as Code: Ansible Automation and Device Validation

| ID | Ch. & Sec. | Type | File Path | Detailed Visual Specification & Generation Prompt |
|:---:|:---:|:---:|:---|:---|
| **G7.1** | Ch 7 §7.2 | Conceptual Diagram | `graphics/ch07/g7-1-iac-idempotency-loop.png` | **Prompt:** "Create an architectural diagram illustrating Infrastructure as Code (IaC) and the principle of idempotency. Center: 'Ansible Automation Engine'. Top: 'Declared Desired State (YAML Playbook: VLAN 100 present)'. Left: 'Device Current State (VLAN 100 missing)'. First execution: Engine inspects state, detects discrepancy, pushes change (`changed: true`), device reaches desired state. Second execution: Engine inspects state, detects desired state already matches actual state, pushes zero changes (`ok: true, changed: false`). Highlight the stability and safety of idempotent reconciliation. Modern flat vector style." |
| **G7.2** | Ch 7 §7.3 | Architecture Comparison | `graphics/ch07/g7-2-agentless-vs-agent-architecture.png` | **Prompt:** "Create a comparative diagram contrasting configuration management architectures. Left panel: 'Agent-Based (Puppet/Chef)' showing a central master server communicating with software agents installed inside the operating systems of target nodes; note: 'Requires third-party daemon, root access, heavy footprint; difficult on locked network switches'. Right panel: 'Agentless (Ansible)' showing an Ansible Control Node communicating directly with standard network devices over native SSH or HTTPS APIs using standard CLI and RESTCONF modules; note: 'Zero software install on managed nodes, lightweight, native network support'. Crisp technical comparison." |
| **G7.3** | Ch 7 §7.4 | Network Topology | `graphics/ch07/g7-3-ansible-topology-inventory.png` | **Prompt:** "Create an Ansible infrastructure topology diagram. Left: 'Ansible Control Node (Linux DEVASC Workstation)' containing `ansible.cfg`, `inventory/hosts.yml`, and `playbooks/`. Show YAML inventory structure grouping devices: `campus: [routers, switches]`. Right: 'Managed Network Fleet' connected via SSH (port 22): Cisco IOS XE Router (`10.10.20.48`), Cisco Catalyst Switch (`10.10.20.50`), and Cisco Nexus 9k Switch (`10.10.20.60`). Show Ansible executing modules via SSH without installing agents. Professional network diagram layout." |
| **G7.4** | Ch 7 §7.4 | Code Anatomy | `graphics/ch07/g7-4-ansible-playbook-anatomy.png` | **Prompt:** "Create an annotated visual breakdown of an Ansible network playbook. Display a syntax-highlighted YAML playbook window with color-coded callouts: (1) 'Play Header' (`hosts: routers`, `connection: network_cli`, `gather_facts: no`), (2) 'Variables Section' (`vars:`), (3) 'Task List' (`tasks:`), (4) 'Module Declaration' (`cisco.ios.ios_config:`), (5) 'Parameters' (`lines:`, `parents:`), (6) 'Register and Assert' (`register: output`, `ansible.builtin.assert:`). Annotate each block explaining its operational role during execution." |
| **G7.5** | Ch 7 §7.5 | Process Flowchart | `graphics/ch07/g7-5-jinja2-templating-flow.png` | **Prompt:** "Create a flowchart illustrating Jinja2 templating for dynamic configuration generation. Left: 'Structured Data Model (YAML/JSON variables defining IP subnets, VLAN IDs, interface descriptions)'. Top: 'Jinja2 Template (`.j2` file containing static Cisco syntax with `{{ variable }}` tags and `{% for %}` loops)'. Center: 'Jinja2 Template Rendering Engine'. Right: 'Rendered Cisco IOS Configuration File (production-ready plain-text config)'. Clean vector arrows showing data flowing into template engine to produce standardized configuration output." |
| **G7.6** | Ch 7 §7.6 | Testing Workflow | `graphics/ch07/g7-6-pyats-verification-workflow.png` | **Prompt:** "Create a technical workflow diagram showing automated network state verification with pyATS and Genie. Stage 1: 'Pre-Change Snapshot' (Genie parses `show ip route` and `show interfaces` into structured JSON baseline). Stage 2: 'Execute Automation Change' (Ansible playbook deploys new route/VLAN). Stage 3: 'Post-Change Snapshot' (Genie captures second structured JSON state). Stage 4: 'Genie Diff Engine' compares pre and post snapshots, highlighting additions (`+`) and unexpected drops (`-`). Stage 5: 'Automated Assertion' passes or triggers rollback. Sleek DevOps workflow graphic." |
| **G7.7** | Ch 7 §7.7 | Change Control Flowchart | `graphics/ch07/g7-7-change-management-rollback-flow.png` | **Prompt:** "Create an enterprise change management flowchart with automated rollback controls. Step 1: Pre-flight health checks and configuration backup (`backup: yes`). Step 2: Push configuration changes via Ansible. Step 3: Run automated verification assertions (ping sweeps, BGP peer status, route table presence). Decision: 'Did all assertions pass?' If YES: Commit configuration to startup-config (`write memory`), log success to change ticket, notify Slack/Webex. If NO: Execute automated rollback task, restore backup configuration from checkpoint, flag emergency ticket, trigger audible alert. High-visibility change management flow." |

---

### Chapter 8: Model-Driven Programmability, Controller APIs, and Capstone Demonstration

| ID | Ch. & Sec. | Type | File Path | Detailed Visual Specification & Generation Prompt |
|:---:|:---:|:---:|:---|:---|
| **G8.1** | Ch 8 §8.2 | Protocol Stack | `graphics/ch08/g8-1-model-driven-stack.png` | **Prompt:** "Create a four-layer protocol stack diagram illustrating the Model-Driven Programmability architecture. Layer 1 (Bottom): 'Transport' (SSH on port 830 for NETCONF, HTTPS on port 443 for RESTCONF). Layer 2: 'Protocol' (NETCONF RFC 6241 with RPC operations vs RESTCONF RFC 8040 with HTTP verbs). Layer 3: 'Data Model' (YANG Models RFC 6020/7950 defining OpenConfig, IETF, and Cisco Native schemas). Layer 4 (Top): 'Encoding' (XML for NETCONF, JSON and XML for RESTCONF). Clean layered diagram, subtle color progression, high technical clarity." |
| **G8.2** | Ch 8 §8.2 | Data Structure Tree | `graphics/ch08/g8-2-yang-tree-hierarchy.png` | **Prompt:** "Create a technical tree diagram illustrating YANG data modeling structures using `ietf-interfaces` as the example. Root node: `module: ietf-interfaces`. Branching into two primary containers: (1) `+--rw interfaces` (read-write configuration) containing list `+--rw interface* [name]` with leaf elements `name (string)`, `description (string)`, `enabled (boolean)`. (2) `+--ro interfaces-state` (read-only operational telemetry) containing operational leaves `oper-status`, `last-change`, and statistics container `in-octets`, `in-errors`. Highlight `rw` vs `ro` distinction. Clean directory-tree visual style." |
| **G8.3** | Ch 8 §8.3 | Sequence & Protocol | `graphics/ch08/g8-3-netconf-architecture-rpc.png` | **Prompt:** "Create a sequence diagram illustrating NETCONF protocol operations between a Python automation script using `ncclient` and a Cisco IOS XE router over SSH port 830. Step 1: SSH connection and `<hello>` capabilities exchange (advertising supported YANG models). Step 2: Client sends `<get-config>` with an XML subtree filter for interface state. Step 3: Router returns `<rpc-reply>` with requested XML data. Step 4: Client sends `<edit-config>` targeting the running datastore. Step 5: Router validates change against YANG schema and returns `<rpc-reply><ok/></rpc-reply>`. Technical sequence layout." |
| **G8.4** | Ch 8 §8.4 | URI Mapping Diagram | `graphics/ch08/g8-4-restconf-uri-mapping.png` | **Prompt:** "Create an architectural diagram illustrating RESTCONF URI mapping and HTTP operation models (RFC 8040). Show root resource URL dissected: `https://10.10.20.48/restconf/data/ietf-interfaces:interfaces/interface=GigabitEthernet1`. Show mapping of standard HTTP verbs to network datastore actions: `GET` -> read configuration/state, `POST` -> create new list entry, `PUT` -> replace entire resource, `PATCH` -> update specific leaf fields, `DELETE` -> remove configuration node. Highlight media type headers: `Content-Type: application/yang-data+json`. High precision technical diagram." |
| **G8.5** | Ch 8 §8.5 | Controller Architecture | `graphics/ch08/g8-5-catalyst-center-architecture.png` | **Prompt:** "Create an architectural diagram contrasting traditional device-by-device management with Cisco Catalyst Center (DNA Center) controller management. Left: Enterprise campus topology with hundreds of access switches, routers, and wireless APs managed by a centralized 'Cisco Catalyst Center Controller'. Right: Developer Application interacting exclusively with the Controller's Intent-Based REST APIs (`/dna/intent/api/v1/network-device`). Show the controller translating developer intent into automated fabric configurations across the physical underlay. Clean enterprise SDN architecture." |
| **G8.6** | Ch 8 §8.6 | Sequence Diagram | `graphics/ch08/g8-6-webex-chatops-flow.png` | **Prompt:** "Create a ChatOps sequence diagram illustrating automated network alerting via a Cisco Webex Teams bot. Step 1: Network monitoring script detects an unauthorized interface state change on a core switch. Step 2: Script formats an alert payload into a rich Webex Adaptive Card (including device name, interface, timestamp, and before/after diff). Step 3: Script sends an authenticated HTTP `POST` request to `https://webexapis.com/v1/messages` using a Bot Bearer Token. Step 4: Webex platform renders the card in an enterprise Network Operations Chat Space with interactive action buttons: 'Acknowledge' and 'Auto-Rollback'. Sleek modern ChatOps graphic." |
| **G8.7** | Ch 8 §8.7 | Solution Architecture | `graphics/ch08/g8-7-capstone-solution-architecture.png` | **Prompt:** "Create a comprehensive master solution architecture diagram for the 'Network Change Automation Service' capstone project. Three horizontal tiers: (1) 'Development & CI/CD Tier': Developer Workstation, Git Repository, GitHub Actions CI Pipeline executing automated unit tests and building a Docker container image. (2) 'Application Execution Tier': Containerized Python automation service running in Docker, integrating `requests`, `ncclient`, and Ansible, backed by a local Mock API fallback. (3) 'Infrastructure Target Tier': Target Cisco CSR1000v router (managed via NETCONF, RESTCONF, and Ansible) and Cisco Webex ChatOps alert channel. High-end enterprise technical architecture." |

---

## 3. Directory Layout & Production Workflow

All generated graphics will be placed in the standalone documentation repository under:
```
net226-oer/
└── modules/
    └── ROOT/
        └── assets/
            └── images/
                ├── ch01/ (G1.1 – G1.6)
                ├── ch02/ (G2.1 – G2.6)
                ├── ch03/ (G3.1 – G3.7)
                ├── ch04/ (G4.1 – G4.6)
                ├── ch05/ (G5.1 – G5.8)
                ├── ch06/ (G6.1 – G6.6)
                ├── ch07/ (G7.1 – G7.7)
                └── ch08/ (G8.1 – G8.7)
```

In AsciiDoc chapter files, figures are referenced using standard Antora syntax:
`image::ch01/g1-1-cli-vs-programmability.png[Box-by-Box CLI vs Network Programmability]`
